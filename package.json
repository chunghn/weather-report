{
  "name": "weather-report",
  "version": "1.0.0",
  "description": "Daily weather report bot for Telegram (繁體中文)",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "bun-types": "^1.3.7",
    "typescript": "^5.3.3"
  }
}
